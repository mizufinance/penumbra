---
phase: 01-compliance-boundary-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/component/compliance/src/audit_records.rs
  - crates/core/component/compliance/src/audit.rs
  - crates/core/component/compliance/src/lib.rs
autonomous: true
requirements:
  - EVID-01
  - EVID-02
  - EVID-03
  - ARCH-01
  - ARCH-02
  - IMPL-01
  - IMPL-02
  - IMPL-04
  - VERI-01
must_haves:
  truths:
    - "Source-backed evidence selects audit/export as the implementation boundary and records registry/state as the rejected alternative."
    - "Audit/export facts that cross internal boundaries are represented as typed records or explicit classification results."
    - "Pure audit projection and import-eligibility behavior can be tested without constructing a SQLite store."
  artifacts:
    - path: "crates/core/component/compliance/src/audit_records.rs"
      provides: "Typed audit/export/import DTOs and pure audit record helper functions"
      contains: "AuditDetectedRef"
    - path: "crates/core/component/compliance/src/audit.rs"
      provides: "Existing audit facade plus implementation evidence notes in tests or module-local comments"
      contains: "export_orbis_pending_scan"
    - path: "crates/core/component/compliance/src/lib.rs"
      provides: "Feature-gated compliance crate export surface"
      contains: "audit_records"
  key_links:
    - from: "crates/core/component/compliance/src/audit.rs"
      to: "crates/core/component/compliance/src/audit_records.rs"
      via: "typed DTO imports and pure helper calls"
      pattern: "audit_records::|crate::audit_records"
    - from: "crates/core/component/compliance/src/lib.rs"
      to: "crates/core/component/compliance/src/audit_records.rs"
      via: "feature-gated module declaration or re-export for moved DTOs"
      pattern: "pub mod audit_records"
---

<objective>
Create the audit/export vertical slice selected by research: record the source evidence, reject registry/state for this MVP, and extract the first pure typed-record boundary from `audit.rs`.

Purpose: Satisfy the phase evidence gate and make audit/export projection and import eligibility directly testable before the effectful facade is fully rewired.
Output: `audit_records.rs` with typed records/classifications, focused pure tests, and stable public audit DTO exports.
</objective>

<execution_context>
@/Users/antoinecyr/Documents/Source/penumbra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/antoinecyr/Documents/Source/penumbra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-compliance-boundary-mvp/01-CONTEXT.md
@.planning/phases/01-compliance-boundary-mvp/01-RESEARCH.md
@.planning/phases/01-compliance-boundary-mvp/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md
@crates/core/component/compliance/src/audit.rs
@crates/core/component/compliance/src/audit_validation.rs
@crates/core/component/compliance/src/scanner/types.rs
@crates/core/component/compliance/src/scanner/storage.rs
@crates/core/component/compliance/src/registry.rs
@crates/core/component/compliance/src/component/state.rs
@crates/core/component/compliance/src/lib.rs

<interfaces>
Key existing contracts:
- `crates/core/component/compliance/src/lib.rs` currently re-exports `AuditDetectedRef`, `AuditScanExport`, and `OrbisAuditEntry` from `audit` behind `#[cfg(feature = "component")]`.
- `scanner/types.rs` owns persisted audit status constants: `AUDIT_STATUS_PENDING`, `AUDIT_STATUS_EVIDENCE_VALID`, `AUDIT_STATUS_EVIDENCE_INVALID`, `AUDIT_STATUS_DECRYPT_FAILED`, `AUDIT_STATUS_AUDIT_COMPLETE`.
- `audit_validation.rs` demonstrates the local pure-helper shape: input struct plus explicit enum status, no persistence side effects.
- `scanner/storage.rs` owns `SqliteScannerStore::lock_conn`; do not open SQLite connections from the new pure module.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure audit record boundary helpers and tests</name>
  <read_first>
    .planning/phases/01-compliance-boundary-mvp/01-CONTEXT.md
    .planning/phases/01-compliance-boundary-mvp/01-RESEARCH.md
    .planning/phases/01-compliance-boundary-mvp/01-PATTERNS.md
    crates/core/component/compliance/src/audit.rs
    crates/core/component/compliance/src/audit_validation.rs
    crates/core/component/compliance/src/scanner/types.rs
    crates/core/component/compliance/src/registry.rs
    crates/core/component/compliance/src/component/state.rs
  </read_first>
  <files>crates/core/component/compliance/src/audit_records.rs</files>
  <behavior>
    - Test 1: an evidence-valid unflagged audit row is classified as eligible for Orbis import completion.
    - Test 2: a flagged row or a non-evidence-valid row is classified as ineligible with the same reason text shape currently recorded by `import_orbis_audit_entries`.
    - Test 3: a detected row projection preserves `height`, hex `tx_hash`, `action_index`, `output_index`, `asset_id`, `is_flagged`, and `flow_type`.
  </behavior>
  <action>Create `audit_records.rs` behind the existing component feature path. Write the behavior tests first, then implement the minimal pure helper surface needed for this task to pass on its own: a typed detected-row input, detected-ref projection, and Orbis import row classification that returns eligible/ineligible decisions with the current reason text shape. Follow `audit_validation.rs` test style. Include a short module-level evidence note: audit/export is selected per D-01/D-03 because `audit.rs` mixes SQLite, export DTO construction, import eligibility, and failure recording; registry/state is rejected for this MVP because `registry.rs` already uses `StateRead`/`StateWrite` and its highest-risk items are deferred storage/security work. Do not implement registration authorization, asset authority, regulated IBC policy enforcement, registry storage indexing, provider traits, or scanner-name mirror modules per D-07/D-09/D-11.</action>
  <verify>
    <automated>cargo test -p penumbra-sdk-compliance --features component audit_records -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `audit_records.rs` exists with pure helper implementations and unit tests that pass within this task.
    - The tests include eligibility, ineligibility, and detected-row projection cases.
    - `rg -n "registration authorization|allowed_channels|AuditStore|RegistryStore|compat|alias" crates/core/component/compliance/src/audit_records.rs` returns no new implementation surface except evidence text that explicitly marks those items rejected/deferred.
  </acceptance_criteria>
  <done>Pure audit-record helpers and tests define the selected audit/export behavior and document registry/state as the rejected alternative without implementing deferred ideas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed audit records and exports</name>
  <read_first>
    crates/core/component/compliance/src/audit_records.rs
    crates/core/component/compliance/src/audit.rs
    crates/core/component/compliance/src/lib.rs
    crates/core/component/compliance/src/scanner/types.rs
    crates/core/component/compliance/src/audit_validation.rs
  </read_first>
  <files>crates/core/component/compliance/src/audit_records.rs, crates/core/component/compliance/src/audit.rs, crates/core/component/compliance/src/lib.rs</files>
  <action>Move or define the public DTOs `AuditDetectedRef`, `AuditScanExport`, and `OrbisAuditEntry` in `audit_records.rs` if that is the smallest stable shape; otherwise keep the DTOs in `audit.rs` and put only pure constructors/classifiers in `audit_records.rs`. Add explicit helper names such as `detected_ref_from_row_parts` and `classify_orbis_import_row` only if their arguments are typed and do not include `rusqlite::Connection`, `rusqlite::Transaction`, or `SqliteScannerStore`. Update `audit.rs` imports to consume the helpers, and update `lib.rs` so the existing public exports remain available under the same names. Per D-04/D-05/D-08, use typed records and pure helpers, not copied scanner module names or new provider traits.</action>
  <verify>
    <automated>cargo test -p penumbra-sdk-compliance --features component audit_records -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `audit_records.rs` contains no SQLite connection, transaction, SQL string, `StateRead`, or `StateWrite` dependency.
    - Existing public names `AuditDetectedRef`, `AuditScanExport`, and `OrbisAuditEntry` remain importable from `penumbra_sdk_compliance` with the `component` feature.
    - `cargo test -p penumbra-sdk-compliance --features component audit_records -- --nocapture` passes.
  </acceptance_criteria>
  <done>`audit_records.rs` provides the pure typed-record boundary, and public audit DTO exports remain stable without compatibility aliases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit import JSON -> audit DTOs | Orbis/imported audit entries are untrusted input before row eligibility checks. |
| scanner SQLite rows -> export DTOs | Durable scanner rows are projected to exported JSON consumed by audit tooling. |
| refactor scope -> deferred security TODOs | Registration authorization and regulated IBC policy are known security issues but outside this refactor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `audit_records::classify_orbis_import_row` | mitigate | Preserve the current evidence-valid unflagged eligibility rule and test flagged, missing, and invalid-status rows. |
| T-01-02 | Information Disclosure | audit export DTO projection | accept | This plan preserves existing export fields only; no new data sources or secrets are added. |
| T-01-03 | Elevation of Privilege | registration authorization TODOs | transfer | Do not implement or alter registration authorization in this plan; keep it deferred to v2 per D-09 and REQUIREMENTS out-of-scope table. |
| T-01-04 | Denial of Service | failure reason strings | accept | This pure module must not add persistence or expand stored failure blobs; persistence remains in `audit.rs`. |
</threat_model>

<verification>
Plan-local checks:
- `cargo test -p penumbra-sdk-compliance --features component audit_records -- --nocapture`
- `cargo check -p penumbra-sdk-compliance --features component --tests`

Source audit:
- GOAL covered: selected audit/export boundary becomes clearer and directly testable.
- REQ covered: EVID-01, EVID-02, EVID-03, ARCH-01, ARCH-02, IMPL-01, IMPL-02, IMPL-04, VERI-01.
- RESEARCH covered: audit/export selected; registry/state rejected; no new provider traits; SQLite effects remain outside `audit_records.rs`.
- CONTEXT covered: D-01 through D-09 and D-11 through D-14 are referenced or constrained by task actions.
</verification>

<success_criteria>
Audit/export has a typed pure-helper module with focused tests, source evidence documents the selected and rejected boundary, and no deferred security/storage item is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-compliance-boundary-mvp/01-01-SUMMARY.md`.
</output>

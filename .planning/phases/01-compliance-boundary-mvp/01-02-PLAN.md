---
phase: 01-compliance-boundary-mvp
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - crates/core/component/compliance/src/audit.rs
  - crates/core/component/compliance/src/audit_records.rs
  - crates/core/component/compliance/src/lib.rs
  - .planning/STATE.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - IMPL-01
  - IMPL-02
  - IMPL-03
  - IMPL-04
  - VERI-01
  - VERI-02
  - VERI-03
must_haves:
  truths:
    - "Existing audit/export public behavior is preserved through the same facade functions."
    - "SQLite access, transactions, and schema knowledge remain at the audit/store edge."
    - "Obsolete private helper paths are removed rather than kept as aliases."
    - "Focused compliance tests and relevant formatting/check commands pass or any broader unrun command is documented."
  artifacts:
    - path: "crates/core/component/compliance/src/audit.rs"
      provides: "Effectful audit facade and SQLite transaction orchestration"
      contains: "SqliteScannerStore"
    - path: "crates/core/component/compliance/src/audit_records.rs"
      provides: "Pure audit record construction and import classification"
      contains: "classify"
    - path: "crates/core/component/compliance/src/lib.rs"
      provides: "Stable feature-gated public audit exports"
      contains: "AuditDetectedRef"
    - path: ".planning/STATE.md"
      provides: "Incremental GSD progress after verified wave execution"
      contains: "Phase 1"
  key_links:
    - from: "crates/core/component/compliance/src/audit.rs"
      to: "crates/core/component/compliance/src/audit_records.rs"
      via: "facade delegates DTO construction and eligibility classification to pure helpers"
      pattern: "audit_records::|crate::audit_records"
    - from: "crates/core/component/compliance/src/audit.rs"
      to: "crates/core/component/compliance/src/scanner/storage.rs"
      via: "existing `SqliteScannerStore::lock_conn` calls stay in effectful facade"
      pattern: "lock_conn\\("
    - from: "crates/core/component/compliance/src/lib.rs"
      to: "public audit callers"
      via: "unchanged `pub use` names for existing DTOs and functions"
      pattern: "AuditDetectedRef.*AuditScanExport.*OrbisAuditEntry"
---

<objective>
Finish the audit/export refactor by wiring the existing facade through the pure record helpers, removing obsolete private paths, and proving behavior with focused and broader compliance checks.

Purpose: Deliver the one-phase MVP outcome: clearer ownership boundaries in the selected compliance area without expanding public behavior or implementing deferred security/storage work.
Output: Refactored audit facade, passing focused tests, formatting/check results, updated GSD state, and plan summary.
</objective>

<execution_context>
@/Users/antoinecyr/Documents/Source/penumbra/.codex/get-shit-done/workflows/execute-plan.md
@/Users/antoinecyr/Documents/Source/penumbra/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-compliance-boundary-mvp/01-CONTEXT.md
@.planning/phases/01-compliance-boundary-mvp/01-RESEARCH.md
@.planning/phases/01-compliance-boundary-mvp/01-PATTERNS.md
@.planning/phases/01-compliance-boundary-mvp/01-01-SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@AGENTS.md
@crates/core/component/compliance/src/audit.rs
@crates/core/component/compliance/src/audit_records.rs
@crates/core/component/compliance/src/lib.rs
@crates/core/component/compliance/src/scanner/storage.rs
@crates/core/component/compliance/src/scanner/types.rs

<interfaces>
Plan 01 creates or updates `audit_records.rs`. Treat that file as the source of truth for pure audit DTO construction and classification.
`audit.rs` remains the owner of `SqliteScannerStore::lock_conn`, SQL strings, transactions, and persistence side effects.
`lib.rs` must preserve existing public function and DTO names unless compile errors prove a smaller direct consumer update is required.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire audit facade through pure helpers</name>
  <read_first>
    .planning/phases/01-compliance-boundary-mvp/01-01-SUMMARY.md
    crates/core/component/compliance/src/audit.rs
    crates/core/component/compliance/src/audit_records.rs
    crates/core/component/compliance/src/lib.rs
    crates/core/component/compliance/src/scanner/storage.rs
    crates/core/component/compliance/src/scanner/types.rs
  </read_first>
  <files>crates/core/component/compliance/src/audit.rs, crates/core/component/compliance/src/audit_records.rs, crates/core/component/compliance/src/lib.rs</files>
  <behavior>
    - Existing tests `orbis_export_requires_valid_evidence`, `orbis_import_requires_valid_evidence`, flagged decryption tests, and rollback/failure tests still pass.
    - Public facade functions `export_orbis_pending_scan`, `import_orbis_audit_entries`, `export_detected_refs`, `export_scan_json`, `export_ledger_rows`, `export_ledger_rows_json`, `validate_and_save_evidence_object`, and `record_evidence_failure` keep behavior.
  </behavior>
  <action>Replace duplicated inline DTO construction and import-eligibility branching in `audit.rs` with calls to the pure helpers created in Plan 01. Keep all SQL queries, transactions, `record_evidence_failure_tx`, `now_unix`, and `SqliteScannerStore::lock_conn` in `audit.rs`. Remove moved private helper definitions instead of keeping aliases per IMPL-03. Preserve existing audit status constants and reason text unless an existing focused test proves a bug; if such a bug appears, add the failing test first and document the behavior change per D-10. Do not add `AuditStore`, `RegistryStore`, or any provider trait for internal SQLite calls per D-07.</action>
  <verify>
    <automated>cargo test -p penumbra-sdk-compliance --features component audit:: -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "trait .*Audit|struct .*Store|pub use .* as|compat|alias" crates/core/component/compliance/src/audit.rs crates/core/component/compliance/src/audit_records.rs` shows no new provider trait, compatibility alias, or speculative store wrapper.
    - `rg -n "lock_conn\\(|unchecked_transaction\\(|SELECT |INSERT |UPDATE " crates/core/component/compliance/src/audit_records.rs` returns no matches.
    - Focused audit tests pass with `cargo test -p penumbra-sdk-compliance --features component audit:: -- --nocapture`.
  </acceptance_criteria>
  <done>The effectful audit facade delegates pure record decisions while retaining all durable SQLite effects at the edge.</done>
</task>

<task type="auto">
  <name>Task 2: Run phase verification and persist GSD progress</name>
  <read_first>
    AGENTS.md
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/REQUIREMENTS.md
    crates/core/component/compliance/src/audit.rs
    crates/core/component/compliance/src/audit_records.rs
    crates/core/component/compliance/src/lib.rs
    justfile
    Cargo.toml
  </read_first>
  <files>.planning/STATE.md</files>
  <action>Run focused and relevant broad checks for D-12/D-13/VERI-02/VERI-03. Required focused commands are `cargo test -p penumbra-sdk-compliance --features component audit_records -- --nocapture`, `cargo test -p penumbra-sdk-compliance --features component audit:: -- --nocapture`, and `cargo test -p penumbra-sdk-compliance --features component`. Required formatting/check commands are `cargo fmt --all -- --check` and `cargo check -p penumbra-sdk-compliance --all-targets --all-features`. If `just check` or another full local CI command is feasible in the environment, run it; if not, record exactly why it was not run in the summary. Update `.planning/STATE.md` after verified completion to reflect Phase 1 planning/execution progress for this wave without marking the phase complete unless all plans are complete.</action>
  <verify>
    <automated>cargo test -p penumbra-sdk-compliance --features component && cargo fmt --all -- --check && cargo check -p penumbra-sdk-compliance --all-targets --all-features</automated>
  </verify>
  <acceptance_criteria>
    - Focused audit-record and audit facade tests pass.
    - `cargo test -p penumbra-sdk-compliance --features component` passes.
    - `cargo fmt --all -- --check` passes.
    - `cargo check -p penumbra-sdk-compliance --all-targets --all-features` passes.
    - Any unrun broad check such as `just check` is named with the exact reason in `01-02-SUMMARY.md`.
    - `.planning/STATE.md` records current phase progress after this verified plan.
  </acceptance_criteria>
  <done>Phase verification evidence is complete, and GSD state is updated for resumability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external audit entry -> SQLite mutation | Imported Orbis entries can cause row status changes only after eligibility checks. |
| SQLite scanner store -> exported JSON | Stored rows are serialized for audit tooling without adding new fields. |
| pure helpers -> durable audit facade | Pure classification must not bypass the persistence checks that guard status transitions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | `import_orbis_audit_entries` | mitigate | Keep row lookup, evidence-valid status gate, unflagged gate, and failure recording before any audit-complete update. |
| T-01-06 | Repudiation | audit failure recording | mitigate | Preserve `record_evidence_failure_tx` calls and stage strings for rejected import rows. |
| T-01-07 | Information Disclosure | `export_ledger_rows_json` and `export_scan_json` | accept | Refactor must preserve existing exported fields only and must not add private key, upload bundle, or decrypted plaintext fields. |
| T-01-08 | Elevation of Privilege | deferred registration and IBC policy enforcement | transfer | Block scope creep: no task may implement D-09 deferred security TODOs; these remain v2 requirements. |
| T-01-09 | Denial of Service | audit evidence failures | accept | This plan preserves existing failure persistence shape and does not add unbounded new storage. |
</threat_model>

<verification>
Overall phase checks:
- `cargo test -p penumbra-sdk-compliance --features component audit_records -- --nocapture`
- `cargo test -p penumbra-sdk-compliance --features component audit:: -- --nocapture`
- `cargo test -p penumbra-sdk-compliance --features component`
- `cargo fmt --all -- --check`
- `cargo check -p penumbra-sdk-compliance --all-targets --all-features`
- `just check` when feasible; otherwise document why it was not run.

Source audit:
- GOAL covered: selected audit/export boundary has clearer ownership with preserved behavior.
- REQ covered: ARCH-01, ARCH-02, ARCH-03, ARCH-04, IMPL-01, IMPL-02, IMPL-03, IMPL-04, VERI-01, VERI-02, VERI-03.
- RESEARCH covered: SQLite effects stay in `audit.rs`; pure helpers own record decisions; no provider traits; registry/state remains rejected.
- CONTEXT covered: D-04 through D-14 shape implementation and verification; D-09/D-11 are enforced by grep gates and threat model.
</verification>

<success_criteria>
The selected audit/export flow is refactored into pure typed record decisions plus an effectful facade, existing behavior is proven by tests, obsolete private paths are removed, and broad verification status is recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/01-compliance-boundary-mvp/01-02-SUMMARY.md`.
</output>
